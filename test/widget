import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test - app uses native FFI which can't run in test environment
    expect(true, isTrue);
  });
}
